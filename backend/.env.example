# Supabase — Settings > API
SUPABASE_URL="https://your-project.supabase.co"
SUPABASE_SERVICE_KEY="your-service-role-key"   # never expose this key to the client

APP_URL="charco://"   # mobile app scheme for OAuth redirect
PORT=3000
NODE_ENV=development
